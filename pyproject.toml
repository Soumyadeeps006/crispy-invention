[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "qs_fraud_qsvm"
version = "0.1.0"
description = "Quantum Support Vector Machine for financial fraud detection using PennyLane"
authors = [{name = "Generated by Antigravity", email = "noreply@example.com"}]
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.9"
dependencies = [
    "pennylane",
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
    "tqdm"
]

[project.urls]
"Homepage" = "https://github.com/Soumyadeeps006/qs_fraud_qsvm"
